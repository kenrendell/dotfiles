# Default config for sway
# Read `man 5 sway` for a complete reference.

### Variables
set $MOD Mod1
set $SYSTEM_MODE 'System: (l)ock, (e)xit, (s)uspend, (h)ibernate, (r)eboot, (p)oweroff'
set $BG_IMAGE '~/Pictures/Wallpapers/desktop-bg.jpg'
set $FONT_NAME Open Sans 8
set $CURSOR_THEME 'Breeze_Snow'
set $CURSOR_SIZE 24

### GTK Themes ###
# You can get the list of keys by running:
# gsettings list-keys org.gnome.desktop.interface
# cursor-size values: 24, 32, 48, 64, 96
exec_always gsettings set org.gnome.desktop.interface {
    gtk-theme 'Arc-Dark'
    icon-theme 'Papirus-Dark'
    cursor-theme $CURSOR_THEME
    cursor-size $CURSOR_SIZE
    font-name 'Open Sans 9'
}

### Idle configuration
exec swayidle -w \
    timeout 300 swaylock \
    timeout 600 'swaymsg "output * dpms off"' \
    resume 'swaymsg "output * dpms on"' \
    before-sleep swaylock

### Status Bar
bar swaybar_command waybar

include {
    /etc/sway/config-vars.d/*
    ~/.config/sway/config.d/*
    /etc/sway/config.d/*
}
