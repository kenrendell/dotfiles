[alias]
	commit-empty = commit --allow-empty-message --message=

[core]
	pager = delta

[pager]
	blame = delta

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true
	line-numbers = true
	syntax-theme = base16

	#blame-code-style = syntax
	#blame-palette = "#1D2025" "#200000"

[merge]
	conflictstyle = diff3

[diff]
	colorMoved = default
